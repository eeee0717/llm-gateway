# llm-gateway

OpenAI 兼容的 LLM 网关：调用方凭 API Key 调用多个上游的模型，网关负责鉴权、转发，并按用量从余额中扣费。

## Language

### 参与方

**调用方**:
持有 API Key、通过网关调用模型的客户端或用户。
_Avoid_: 下游、客户端

**上游**:
网关把请求转发到的模型服务端点，即某家供应商的 OpenAI 兼容接口。
_Avoid_: 渠道、channel

**模型**:
调用方在请求中指定的模型名；每个模型固定对应一个上游和一组单价，对外名称与上游的模型名一致。

### 凭证

**API Key**:
网关发给调用方的凭证，决定鉴权结果与扣费对象。
_Avoid_: 令牌、网关 Key

**禁用**:
管理员停用某个 API Key；被禁用的 Key 无法通过鉴权，余额保留。
_Avoid_: 删除、吊销

**上游密钥**:
网关访问上游时使用的凭证，只在网关与上游之间出现，调用方永远看不到。
_Avoid_: API Key、渠道密钥

**管理员密钥**:
访问管理接口的凭证，用于创建 API Key、充值和禁用；与 API Key 互不通用。
_Avoid_: admin token、超级 Key

### 计量

**token**:
LLM 用量的计量单位，分为 prompt token 与 completion token；在本项目中不指代任何凭证。

**用量**:
一次请求消耗的 prompt token 数与 completion token 数，来源要么是上游报告，要么是估算用量。

**估算用量**:
上游没有报告用量时（例如中途断开），网关根据请求内容和已转发的内容推算出的用量。
_Avoid_: 预估

**单价**:
模型每百万 token 的价格，prompt token 与 completion token 分别定价，即输入单价与输出单价。

**费用**:
一次请求的用量按单价折算出的金额。
_Avoid_: 消耗、花费

### 余额

**余额**:
API Key 当前可用的金额。
_Avoid_: 额度、quota、credit

**充值**:
管理员增加某个 API Key 余额的操作。

**预扣**:
请求转发前，按估算的最大费用先从余额中扣下的金额；余额不够预扣时，请求被拒绝。
_Avoid_: 冻结、占用

**结算**:
请求结束后按实际费用对预扣多退少补，使余额最终只减少这次请求的费用。
_Avoid_: 扣费、对账

**透支**:
结算后余额小于零的状态，只可能由估算的 prompt token 偏低造成。

**用量记录**:
每次结算后留下的一条记录，写明 API Key、模型、用量、费用，以及用量是上游报告的还是估算的。

### 请求

**中途断开**:
调用方在响应完成前断开连接。
_Avoid_: 客户端取消、client gone
