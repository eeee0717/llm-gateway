# API Key 只存 SHA-256 哈希

API Key 的格式是 `sk-` 前缀加至少 32 字节的随机数（`crypto/rand`）。数据库只存它的 SHA-256 哈希，明文只在创建时返回一次，丢失后只能新建。

不用 bcrypt 这类慢哈希。慢哈希故意算得慢，是为了抵御针对低熵口令的字典攻击；API Key 有 256 位熵，穷举本来就不可行，慢哈希只会拖慢每一个请求的鉴权。另外慢哈希带随机盐，同一个 Key 每次算出的值都不同，没法按哈希值建唯一索引；SHA-256 的结果是确定的，鉴权就是一次按索引的等值查询。
