# 鉴权缓存

## 问题

每个请求都要先认出调用方是谁。Key 的明文不落库，鉴权就是拿明文的 SHA-256 去 `api_keys` 查一行。这是一次等值查询，走唯一索引，本身不慢，但它出现在每一个请求的最前面，而且和后面的预扣、结算抢同一个数据库连接池。

同时还有一条硬要求：**管理员禁用一个 Key 之后，下一个请求就该被拒**，不能等缓存过期。网关会跑多个实例，禁用是在其中一个实例上发生的。

## 方案

**Cache-Aside，缓存放在 Redis 里**（`internal/apikey/cache.go`）。鉴权先查 `auth:<hash>`，没有就查数据库再写回，TTL 5 分钟。

**缓存里只放 `Identity`**：Key 的 ID、禁用状态、限流额度。余额不进缓存——余额只有 PostgreSQL 一份，见 [ADR-0002](../adr/0002-balance-in-postgres-only.md)。这条界线让缓存变成纯粹的加速：里面的数据全丢了也只是慢一点，不会算错账。

**查不到的 Key 也记进缓存**，TTL 30 秒。不然拿随机 Key 反复打过来，每个请求都要查一趟数据库，缓存等于没有。

**禁用之后立刻删缓存**（`internal/admin/admin.go`）。顺序是先改数据库、再删 Redis 的键。删这一步不跟着管理员的请求取消——数据库已经改完了，管理员这时断开连接的话，缓存就留着不删。删的是那份共享的缓存，所以不用在实例之间广播：另一个实例的下一个请求发现缓存没了，自然回源，读到的就是禁用后的状态。改额度和创建 Key 时同样删一次——创建之前可能有人拿这个哈希查过，缓存里记着"不存在"。

**Redis 出任何问题都当缓存不存在**：读失败、写失败、存的值解析不出来，都只记一条日志，然后走数据库。Redis 整个连不上时网关照常工作，有测试守着（`TestGatewayKeepsWorkingWithoutRedis`）。

**Redis 的超时压到 100 毫秒，关掉重试，并显式设连接池的等待时间。** 这条比"失败时放行"更要紧：放行的前提是很快就知道 Redis 不行了。

`TestGatewayDoesNotStallOnAnUnresponsiveRedis` 用一个只接受连接、之后什么也不回的监听冒充 Redis，断言一个请求在一秒内走完（实测约 0.37 秒，一个请求最多碰三次 Redis）。把超时改回 go-redis 的默认值再跑，同一个请求要 **15 秒**——5 秒超时乘以三次访问。fail-open 在默认配置下会变成 fail-slow。

连接池的等待时间要单独设：不设的话 go-redis 把它推导成"读超时 + 1 秒"，等于在那 100 毫秒之上又加了一秒，而连接池被占满正是 Redis 不响应时最容易出现的情况。

**收口断言**（`cmd/gateway/main_test.go`）：两个实例共用一套 Redis，各跑一次请求把 Key 灌进缓存，在第一个实例上禁用，然后两个实例的下一个请求都必须返回 401 `key_disabled`。断言里特意区分了错误码——预扣的条件 UPDATE 也会挡下被禁用的 Key，但那时返回的是 429 `insufficient_quota`，说明禁用是被计费兜住的，而不是鉴权挡下的。

光有这一条还不够：它在"根本没接缓存"的情况下同样成立（每次都读数据库，禁用天然立刻生效）。所以另有两条专门钉住缓存本身——**绕过管理接口直接改库，请求仍然被放行**（证明缓存确实挡在数据库前面，也把上面那个陈旧窗口写成了可执行的断言），以及**改额度之后下一个请求就按新额度算**。把缓存从装配里摘掉，或者删掉改额度后的失效，这两条分别会失败。

## 实测：这层缓存到底省了多少

把缓存从装配里摘掉（`apikey.Middleware` 直接接 `Store`），同一台机器上跑同一组压测，答案是**几乎没省**。

先看纯鉴权的那条路径。`GET /v1/models` 只走鉴权和限流，不预扣也不结算，写库的开销不会盖住信号。同一个 Key 连打 3000 次（另有 200 次热身不计）：

| 鉴权走哪里 | min | p50 | p90 | p99 | 平均 |
|---|---|---|---|---|---|
| PostgreSQL | 0.189ms | 0.219ms | 0.245ms | 0.317ms | 0.228ms |
| Redis 缓存 | 0.171ms | 0.199ms | 0.221ms | 0.307ms | **0.204ms** |

**快 0.02 毫秒，约 10%。** 端到端就更看不出来了：`POST /v1/chat/completions` 的吞吐 3065 对 3034 QPS（1%），首 token 延迟的 min 是 +2.0ms 对 +2.2ms，落在两次运行的抖动里。

原因在数据库那一侧。鉴权是一条走唯一索引的等值查询，表又全在内存里：

```
Index Scan using api_keys_key_hash_key on api_keys
  (actual time=0.011..0.011 rows=0 loops=1)
  Buffers: shared hit=2
```

**0.011 毫秒。** 一次 Redis GET 光是本机 TCP 往返就要这个数的十几倍，两边一减，剩下的就是那 20 微秒。

所以要诚实地记下来：**按现在这个规模，鉴权缓存不是一个性能优化。** 它真正买到的是另外两样东西——把读流量从 PostgreSQL 挪到 Redis，而 PostgreSQL 在这套系统里是写的瓶颈（压测时 CPU 占到 95%）、连接池只有 20 条、也是最难水平扩的那一层；以及 Key 表涨大、或者数据库不在同一台机器上时，这条查询不会一直是 0.011 毫秒。

代价则是实打实的：多一处要维护的一致性（禁用和改额度都要记得删缓存，回填还有竞态），多一个故障源（Redis 不响应时要能很快放行，见下）。**没有缓存的话，"禁用后立即生效"本来是天然成立的。**

这笔账摆出来是想说清楚：这层缓存是按"将来会有更多实例、更大的 Key 表"来设计的，不是当下的性能需求。先测再加会是更好的顺序。

## 取舍

- **删缓存失败只记日志，管理接口照样返回成功。** 数据库已经改完了，改动最终会生效，只是要等一个 TTL，所以没必要让管理员看到一个失败。代价是"立刻生效"在这种时候会静默退化成"五分钟内生效"，能看出来的只有那条 `auth cache delete failed` 日志。
- **回填有竞态，没有消除它。** 一个请求读到旧值、还没写回 Redis 时，管理员禁用并删了缓存，这个请求随后把旧值写了回去，禁用就要等一个 TTL 才生效。消除它要用版本号或者 `WATCH`，而代价不值得：被禁用的 Key 即使穿过鉴权，也会在预扣的条件 UPDATE（`WHERE ... AND NOT disabled`）上被挡下，只是调用方收到的是 429 而不是 401。钱不会错，错的只是错误码。
- **TTL 5 分钟，不做主动刷新。** 充值不删缓存——余额不在缓存里，充完立刻就能用。只有改变身份的操作（禁用、改额度）才删。
- **缓存"不存在"的代价是新建的 Key 可能要等 30 秒。** 只有"先查后建"这个顺序才会踩到，而管理接口创建 Key 时会顺手删掉这个记号，所以实际上碰不到。
- **不做进程内缓存。** 加一层本地缓存能省掉 Redis 的往返，但禁用就得在实例之间广播（发布订阅或者版本号轮询），"立即失效"这条断言会难保证得多。何况按上面测出来的数，本地缓存能省的也就是那 0.2 毫秒里的大部分，而它要省的这一段本身就不是瓶颈。

## 延伸问题

- Redis 挂掉的瞬间，所有请求一起回源数据库，会不会把数据库压垮？会有一个尖峰。挡它的常见做法是 singleflight：同一个哈希只放一个请求去查数据库，其余等结果。现在没做，因为鉴权查询走唯一索引、很轻。
- 缓存里存 JSON 是不是浪费？一个 `Identity` 编码出来不到 50 字节，省的那点空间不值得换一个自定义编码。
- 要不要缓存整个 `Key`（含余额）？不要。那样余额就有两份，得回答"以谁为准"，而这正是 ADR-0002 要避免的。
